apiVersion: v2
name: kdp-k8ssandra
description: PostgreSQL operator CloudnativePG.

type: application

# This is the chart version. This version number should be incremented each time you make changes
# to the chart and its templates, including the app version.
# Versions are expected to follow Semantic Versioning (https://semver.org/)
version: 0.0.1
appVersion: "1.26.0" # Strimzi Kafka operator version.

# pull in external charts via dependencies:
# Strimzi Operator helm chart
dependencies:
  - name: k8ssandra-operator
    version: 1.26.0
    repository: https://helm.k8ssandra.io/stable
